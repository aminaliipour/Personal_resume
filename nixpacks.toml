[phases.setup]
nixPkgs = ["nodejs-20_x", "python310"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["node scripts/init-db.js || true", "npm run build"]

[start]
cmd = "sh -c 'node scripts/init-db.js || true; node scripts/seed-db.js || true; npm start'"

